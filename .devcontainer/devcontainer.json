{
  "name": "Outlier-X Codespace",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker"
      ]
    }
  },
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.11"
    }
  },
  "postCreateCommand": "apt-get update && apt-get install -y ruff black pytest && source .venv/bin/activate && pip install -r requirements.txt && pytest"
}