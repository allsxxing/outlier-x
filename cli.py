#!/usr/bin/env python3
"""Entry point for the Outlier-X CLI.

This module serves as the main entry point for running the CLI as documented
in the README. It imports and executes the CLI from src.main.
"""

from src.main import cli

if __name__ == "__main__":
    cli(obj={})
